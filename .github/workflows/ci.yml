name: CI Pipeline

on:
  push:
    branches:
      - master

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run CI test
        run: |
          echo "GitHub Actions CI pipeline is running"
          echo "GitHub Actions CI/CD test passed"
